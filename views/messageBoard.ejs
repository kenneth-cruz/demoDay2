<!doctype html>
<html>
<!-- links to all the files main css, bootstrap etc -->
<head>
  <title>WorldNewsFlash</title>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
  <link rel="stylesheet" href="messageBoard.css">
</head>

<body>

    <div class="page-header text-center">
      <h1><span> </span> WorldNewsFlash <br> <i class="fas fa-book"></i> Knowledge.Exchange Page <i class="fas fa-book"></i> </h1>
      <a href="/logout" class="btn btn-default btn-sm">Logout</a>
      <a href="/" class="btn btn-default btn-sm" >Home</a>
      <!-- logout link -->
    </div>

<section class = "mainContainer">
        <h3> Current Comments ... on Current Events! </h3>
        <section class="corkBoard">
          <ul class="messages">
            <% for(var i=0; i< messages.length; i++) {%>
            <% if (user.local.email == messages[i].name) { %>
            <li class="message">
              <span><%= messages[i].name%></span>
              <span><%= messages[i].msg %></span>
              <span><%= messages[i].thumbUp -= messages[i].thumbDown %></span>
              <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
              <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
            <% } %>
            <% } %>
          </ul>
        </section>
        <h2>Join the Forum: Choose a Topic, Make a Comment !</h2>
        <% if (user.local.email) { %>
        <form action="/messages" method="POST">
          <input type="text" value="<%= user.local.email %>" name="name">
          <input type="text" placeholder="message" name="msg">
          <button type="submit">Submit</button>
        </form>
      </div>
      <% } %>
</section>

    <script src="https://kit.fontawesome.com/077191d827.js" crossorigin="anonymous"></script>
    <script src="main.js"></script>

</body>

</html>
